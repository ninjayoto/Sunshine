<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    tools:context=".MainActivity">

    <!-- these are the menu items, they are ordered in the same order they appear here
     but if you want to change the order you change the OrderInCategory value
     showAsAction value is to choose either to place the menu item at the action bar
     (for frequently used options) or just make it part of the menu-->
    <item android:id="@+id/action_settings"
          android:title="@string/action_settings"
          android:orderInCategory="100"
          app:showAsAction="never" />
</menu>
